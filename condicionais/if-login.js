// Verificar se LOGIN e SENHA
// correspondente ao mesmo tempo ( && )

const login = 'superman'
const senha = 'segredo120'

 if (LOGIN == 'superman' && SENHA  == 'segredo120') {
    console.log ('acessar permitido')
 } else {
    console,warn ('verifique dados') 
 }
